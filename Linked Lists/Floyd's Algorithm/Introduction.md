## Classes of Pointers
- **Two pointers with the same speed and the same starting position** will always be at the same node on every step. → Not useful.
- **Two pointers with different speeds and the same starting position** will eventually meet if a cycle exists. → This is the key idea behind **Floyd's Cycle Detection Algorithm**.
- **Two pointers with the same speed but different starting positions** → To be seen!
